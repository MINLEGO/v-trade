{
  "schema_version": "predictionarena-tools-v1",
  "evidence_cutoff": "2026-07-13",
  "notice": "Names and observed argument keys come from public traces. Types, required fields, bounds and outputs are inferred V-Trade contracts.",
  "$defs": {
    "market_discovery_card": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "market_ref": {"type": "string", "minLength": 1},
        "question": {"type": "string"},
        "closes_at": {"type": ["string", "null"]},
        "volume_24h_micros": {"type": "integer"},
        "liquidity_micros": {"type": "integer"},
        "status": {"type": "string"},
        "tradeable": {"type": "boolean"},
        "competitive": {"type": "number"},
        "tag_names": {
          "type": "array",
          "items": {"type": "string"}
        },
        "outcomes": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": true,
            "properties": {
              "name": {"type": "string"},
              "indicative_price": {"type": "string"}
            },
            "required": ["name", "indicative_price"]
          }
        }
      },
      "required": [
        "market_ref", "question", "closes_at", "volume_24h_micros",
        "liquidity_micros", "status", "tradeable", "competitive", "tag_names", "outcomes"
      ]
    },
    "market_discovery_page": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "as_of": {"type": "string", "format": "date-time"},
        "markets": {
          "type": "array",
          "items": {"$ref": "#/$defs/market_discovery_card"}
        },
        "next_cursor": {"type": ["string", "null"]},
        "has_more": {"type": "boolean"},
        "payload_truncated": {"type": "boolean"}
      },
      "required": ["as_of", "markets", "next_cursor", "has_more", "payload_truncated"]
    },
    "event_group": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "event_id": {"type": "string", "minLength": 1},
        "markets": {
          "type": "array",
          "items": {"$ref": "#/$defs/market_discovery_card"}
        },
        "volume_24hr_micros": {"type": "integer"},
        "total_volume_micros": {"type": "integer"},
        "newest_market_created_at": {"type": ["string", "null"]}
      },
      "required": [
        "event_id", "markets", "volume_24hr_micros",
        "total_volume_micros", "newest_market_created_at"
      ]
    },
    "event_discovery_page": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "as_of": {"type": "string", "format": "date-time"},
        "events": {
          "type": "array",
          "items": {"$ref": "#/$defs/event_group"}
        },
        "next_cursor": {"type": ["string", "null"]},
        "has_more": {"type": "boolean"},
        "payload_truncated": {"type": "boolean"}
      },
      "required": ["as_of", "events", "next_cursor", "has_more", "payload_truncated"]
    },
    "orderbook_level": {
      "type": "object",
      "additionalProperties": true,
      "properties": {
        "price": {"type": "string"},
        "size": {"type": "string"}
      },
      "required": ["price", "size"]
    },
    "position": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "position_id": {"type": "string", "format": "uuid"},
        "market_id": {"type": "string", "format": "uuid"},
        "market_question": {"type": "string"},
        "outcome_id": {"type": "string", "format": "uuid"},
        "venue_token_id": {"type": "string", "minLength": 1},
        "outcome": {"type": "string"},
        "shares": {"type": "string"},
        "average_cost": {"type": "string"},
        "cost_basis_micros": {"type": "integer"},
        "realized_pnl_micros": {"type": "integer"},
        "updated_at": {"type": "string", "format": "date-time"}
      },
      "required": [
        "position_id", "market_id", "market_question", "outcome_id", "venue_token_id",
        "outcome", "shares", "average_cost", "cost_basis_micros",
        "realized_pnl_micros", "updated_at"
      ]
    },
    "portfolio_after": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "version": {"type": "integer", "minimum": 0},
        "cash_micros": {"type": "integer"},
        "affected_position": {
          "type": ["object", "null"],
          "additionalProperties": false,
          "properties": {
            "outcome_id": {"type": "string"},
            "shares": {"type": "string"},
            "average_cost": {"type": "string"},
            "cost_basis_micros": {"type": "integer"}
          },
          "required": ["outcome_id", "shares", "average_cost", "cost_basis_micros"]
        }
      },
      "required": ["version", "cash_micros", "affected_position"]
    },
    "belief": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "id": {"type": "string", "format": "uuid"},
        "active": {"type": "boolean"},
        "confidence": {"type": "string"},
        "content": {"type": "string"},
        "category": {
          "type": "string",
          "enum": [
            "event_analysis", "trading_strategy", "market_sentiment",
            "market_structure", "risk_assessment"
          ]
        },
        "evidence": {"type": "array", "items": {"type": "string"}},
        "created_at": {"type": "string", "format": "date-time"}
      },
      "required": ["id", "confidence", "content", "category", "evidence", "created_at"]
    },
    "plan": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "id": {"type": "string", "format": "uuid"},
        "plan_type": {"type": "string", "enum": ["long_term", "next_cycle"]},
        "status": {"type": "string", "enum": ["active", "completed", "cancelled"]},
        "due_at": {"type": ["string", "null"], "format": "date-time"},
        "content": {"type": "string"},
        "created_at": {"type": "string", "format": "date-time"}
      },
      "required": ["id", "plan_type", "status", "due_at", "content", "created_at"]
    }
  },
  "tools": [
    {
      "name": "get_newest_markets",
      "description": "List open, tradeable markets created within the last `hours_back` hours of the current cycle cutoff.\n\nUse this tool to identify newly listed individual markets that may not yet have been widely researched or efficiently priced.\n\nResults will be ordered primarily by creation time from newest to oldest, with market ID as a secondary tie-breaker.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "cursor": {
            "type": "string",
            "minLength": 1
          },
          "hours_back": {
            "type": "integer",
            "minimum": 0,
            "default": 24
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 20
          },
          "min_liquidity": {
            "type": "number",
            "minimum": 0
          },
          "min_volume_24hr": {
            "type": "number",
            "minimum": 0
          }
        },
        "required": []
      },
      "output_schema": {
        "$ref": "#/$defs/market_discovery_page"
      }
    },
    {
      "name": "discover_by_time_remaining",
      "description": "Find open, tradeable markets whose `closes_at` time is between `hours_min` and `hours_max` hours after the current cycle cutoff. Use this tool to locate markets approaching closure or markets within a specific trading horizon.\n\nResults are ordered by remaining time ascending, with the soonest-closing markets first.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "cursor": {
            "type": "string",
            "minLength": 1
          },
          "hours_min": {
            "type": "number",
            "minimum": 0
          },
          "hours_max": {
            "type": "number",
            "minimum": 0
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 20
          },
          "min_liquidity": {
            "type": "number",
            "minimum": 0
          }
        },
        "required": []
      },
      "output_schema": {
        "$ref": "#/$defs/market_discovery_page"
      }
    },
    {
      "name": "discover_events",
      "description": "Search for groups of related markets by event. The optional(s) `keyword` are matched case-insensitively against market questions and market metadata. Markets are grouped by `event_id`, and event groups are ordered by their aggregated 24-hour volume.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "cursor": {
            "type": "string",
            "minLength": 1
          },
          "keyword": {
            "type": [
              "string",
              "array"
            ],
            "items": {
              "type": "string",
              "minLength": 1
            },
            "minItems": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 20
          },
          "min_liquidity": {
            "type": "number",
            "minimum": 0
          },
          "min_volume_24hr": {
            "type": "number",
            "minimum": 0
          }
        },
        "required": []
      },
      "output_schema": {
        "$ref": "#/$defs/event_discovery_page"
      }
    },
    {
      "name": "list_top_events",
      "description": "List groups of related markets ordered by their aggregated total historical volume. Use this tool to identify the largest or most established events in the frozen market universe.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "cursor": {
            "type": "string",
            "minLength": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 20
          },
          "min_liquidity": {
            "type": "number",
            "minimum": 0
          },
          "min_volume_24hr": {
            "type": "number",
            "minimum": 0
          }
        },
        "required": []
      },
      "output_schema": {
        "$ref": "#/$defs/event_discovery_page"
      }
    },
    {
      "name": "get_market_details",
      "description": "Retrieve the complete frozen record for one market as of the current cycle cutoff. Supply exactly one of `market_ref`, `market_id`, or `slug`. Prefer the `market_ref` returned by discovery tools when available.\n\nThe result includes the market question, official resolution rules, opening and closing times, status, tradeability, volume, liquidity, metadata, and all outcomes with their venue token identifiers. Treat the resolution rules and outcome mapping as authoritative for side selection.\n\nPrices contained in market metadata or outcomes are indicative snapshots, not executable quotes. Call `get_orderbook` before trading.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "market_ref": {
            "type": "string",
            "minLength": 1
          },
          "market_id": {
            "type": "string",
            "minLength": 1
          },
          "slug": {
            "type": "string",
            "minLength": 1
          }
        },
        "required": [],
        "oneOf": [
          {"required": ["market_ref"]},
          {"required": ["market_id"]},
          {"required": ["slug"]}
        ]
      },
      "output_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "as_of": {"type": "string", "format": "date-time"},
          "market": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "id": {"type": "string"},
              "venue_market_id": {"type": "string"},
              "market_ref": {"type": "string", "minLength": 1},
              "slug": {"type": "string"},
              "canonical_slug": {"type": "string"},
              "event_id": {"type": "string"},
              "question": {"type": "string"},
              "resolution_rules": {"type": "string"},
              "opens_at": {"type": ["string", "null"]},
              "closes_at": {"type": ["string", "null"]},
              "volume_micros": {"type": "integer"},
              "liquidity_micros": {"type": "integer"},
              "status": {"type": "string"},
              "tradeable": {"type": "boolean"},
              "metadata": {"type": "object", "additionalProperties": true},
              "outcomes": {
                "type": "array",
                "items": {"type": "object", "additionalProperties": true}
              }
            },
            "required": [
              "id", "venue_market_id", "market_ref", "slug", "canonical_slug", "event_id",
              "question", "resolution_rules", "opens_at", "closes_at", "volume_micros",
              "liquidity_micros", "status", "tradeable", "metadata", "outcomes"
            ]
          }
        },
        "required": ["as_of", "market"]
      }
    },
    {
      "name": "web_search",
      "description": "Search the external web for current evidence relevant to an event, market thesis, probability estimate, resolution rule, forecast, or catalyst. The tool returns up to ten search results with available titles, snippets, URLs and publication timestamps.\n\nYou can balance the number of result and their quality using `num_results` and `max_highlight_length`. Note that to avoid truncated results, max_highlight_length × num_results must not exceed 15000 character and will return an error otherwise. The optional `start_published_date` and `end_published_date` arguments define the publication-date range. Each accepts a non-negative number of days back from now or a `YYYY-MM-DD` date; they default to 30 and 0 respectively.\n\nCheck the publication date and ensure that evidence was available before the cycle’s data cutoff. Prefer primary or authoritative sources, distinguish independent sources from repeated reporting, and actively search for evidence that could disconfirm the thesis.\n\nSearch-result snippets may be incomplete or misleading. When the information is critical, try using `fetch_webpage` to retrieve more details and never let external reporting override the market’s official resolution rules.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "query": {
            "type": "string",
            "minLength": 1
          },
          "max_highlight_length": {
            "type": "integer",
            "minimum": 1,
            "maximum": 15000,
            "default": 1500,
            "description": "Maximum characters returned in highlights per result. Num_results × max_highlight_length must not exceed 15000."
          },
          "num_results": {
            "type": "integer",
            "minimum": 1,
            "maximum": 10,
            "default": 10
          },
          "start_published_date": {
            "type": [
              "integer",
              "string"
            ],
            "minimum": 0,
            "format": "date",
            "default": 30,
            "description": "Range start as non-negative days back from now or YYYY-MM-DD."
          },
          "end_published_date": {
            "type": [
              "integer",
              "string"
            ],
            "minimum": 0,
            "format": "date",
            "default": 0,
            "description": "Range end as non-negative days back from now or YYYY-MM-DD."
          }
        },
        "required": [
          "query"
        ]
      },
      "output_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "query": {
            "type": "string"
          },
          "results": {
            "type": "array",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "properties": {
                "title": {
                  "type": "string"
                },
                "url": {
                  "type": "string"
                },
                "published_at": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "content": {
                  "type": "string"
                }
              },
              "required": [
                "title",
                "url",
                "published_at",
                "content"
              ]
            }
          }
        },
        "required": [
          "query",
          "results"
        ]
      }
    },
    {
      "name": "fetch_webpage",
      "description": "Retrieve the readable content of a specific public webpage URL. Use this tool when a search result, market rule, official announcement, report, dataset page or primary source must be inspected directly. Prefer the original or authoritative source when a search result points to one.\n\nOptionally set `result_type` to `full_text` for the full page text or `highlights` for focused excerpts; When using `highlights`, `highlight_query` can guide the snippet selection; it must be omitted or set to `null` for `full_text`. `max_length` defaults to 4000 characters and cannot exceed 12000.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "url": {
            "type": "string",
            "minLength": 1
          },
          "result_type": {
            "type": "string",
            "enum": [
              "full_text",
              "highlights"
            ],
            "default": "highlights"
          },
          "highlight_query": {
            "type": [
              "string",
              "null"
            ],
            "minLength": 1,
            "default": null,
            "description": "Query guiding highlight selection. It is invalid when result_type is full_text."
          },
          "max_length": {
            "type": "integer",
            "minimum": 1,
            "maximum": 12000,
            "default": 4000,
            "description": "Maximum characters returned from the webpage content."
          }
        },
        "required": [
          "url"
        ]
      },
      "output_schema": {
        "oneOf": [
          {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "title": {"type": "string"},
              "url": {"type": "string"},
              "published_at": {"type": ["string", "null"]},
              "author": {"type": ["string", "null"]},
              "full_text": {"type": "string"}
            },
            "required": ["title", "url", "published_at", "author", "full_text"]
          },
          {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "title": {"type": "string"},
              "url": {"type": "string"},
              "published_at": {"type": ["string", "null"]},
              "author": {"type": ["string", "null"]},
              "highlights": {"type": "array", "items": {"type": "string"}}
            },
            "required": ["title", "url", "published_at", "author", "highlights"]
          }
        ]
      }
    },
    {
      "name": "get_orderbook",
      "description": "Retrieve the latest valid frozen order-book snapshot for one outcome as of the current cycle cutoff. Supply exactly one of `venue_token_id`, `token_id`, or `outcome_id`.\n\nThe result contains up to five bid and ask levels, the best bid, the best ask, observation timestamps and the snapshot identifier. Use asks to evaluate a BUY and bids to evaluate a SELL. Inspect the displayed depth rather than assuming the full requested size can execute at the best price.\n\nThe tool rejects missing, causally invalid or stale books. A missing or rejected result means that executable liquidity cannot currently be verified and the outcome should not be traded unless a later valid book is obtained.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "venue_token_id": {
            "type": "string",
            "minLength": 1
          },
          "outcome_id": {
            "type": "string",
            "minLength": 1
          },
          "token_id": {
            "type": "string",
            "minLength": 1
          }
        },
        "required": [],
        "oneOf": [
          {"required": ["venue_token_id"]},
          {"required": ["outcome_id"]},
          {"required": ["token_id"]}
        ]
      },
      "output_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "as_of": {"type": "string", "format": "date-time"},
          "snapshot_id": {"type": "string"},
          "observed_at": {"type": "string", "format": "date-time"},
          "source_created_at": {"type": ["string", "null"], "format": "date-time"},
          "lookup": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "venue_token_id": {"type": "string", "minLength": 1},
              "outcome_id": {"type": "string", "minLength": 1},
              "token_id": {"type": "string", "minLength": 1}
            },
            "oneOf": [
              {"required": ["venue_token_id"]},
              {"required": ["outcome_id"]},
              {"required": ["token_id"]}
            ]
          },
          "bids": {"type": "array", "items": {"$ref": "#/$defs/orderbook_level"}},
          "asks": {"type": "array", "items": {"$ref": "#/$defs/orderbook_level"}},
          "best_bid": {"type": ["string", "null"]},
          "best_ask": {"type": ["string", "null"]},
          "raw_sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
          "depth": {"const": 5}
        },
        "required": [
          "as_of", "snapshot_id", "observed_at", "source_created_at", "lookup",
          "bids", "asks", "best_bid", "best_ask", "raw_sha256", "depth"
        ]
      }
    },
    {
      "name": "discover_by_price_volatility",
      "description": "Find open, tradeable markets whose recorded price volatility is at least `min_volatility`. Volatility is currently defined as the larger absolute value of the recorded one-hour and one-day price changes.\n\nResults are ordered by volatility magnitude descending, then by total market volume.\n\nUse this tool to identify markets with recent repricing or potential catalysts.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "cursor": {
            "type": "string",
            "minLength": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 20
          },
          "min_liquidity": {
            "type": "number",
            "minimum": 0
          },
          "min_volatility": {
            "type": "number",
            "minimum": 0
          }
        },
        "required": []
      },
      "output_schema": {
        "$ref": "#/$defs/market_discovery_page"
      }
    },
    {
      "name": "get_event_markets",
      "description": "Retrieve the open, tradeable markets associated with one event. Supply the required `event_id`, which may match the internal event identifier or the venue event identifier.\n\nMarkets are ordered primarily by total historical volume. Use this tool after `discover_events`, `list_top_events` or `get_newest_events` to compare related outcomes and identify mutually related or correlated positions.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "event_id": {
            "type": "string",
            "minLength": 1
          },
          "cursor": {
            "type": "string",
            "minLength": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 20
          }
        },
        "required": [
          "event_id"
        ]
      },
      "output_schema": {
        "$ref": "#/$defs/market_discovery_page"
      }
    },
    {
      "name": "get_newest_events",
      "description": "List event groups ordered by the creation time of their newest associated market. Use this tool to identify newly added events that may not yet have been widely researched or efficiently priced.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "cursor": {
            "type": "string",
            "minLength": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 20
          },
          "min_liquidity": {
            "type": "number",
            "minimum": 0
          }
        },
        "required": []
      },
      "output_schema": {
        "$ref": "#/$defs/event_discovery_page"
      }
    },
    {
      "name": "get_all_active_markets",
      "description": "List all open and tradeable markets included in the current cycle’s frozen universe.\n\nResults are ordered primarily by total historical volume and liquidity. Use this tool for exhaustive or broad discovery when narrower discovery tools are not appropriate.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "cursor": {
            "type": "string",
            "minLength": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 20
          },
          "min_liquidity": {
            "type": "number",
            "minimum": 0
          },
          "min_volume_24hr": {
            "type": "number",
            "minimum": 0
          }
        },
        "required": []
      },
      "output_schema": {
        "$ref": "#/$defs/market_discovery_page"
      }
    },
    {
      "name": "discover_by_volume_trend",
      "description": "Find open, tradeable markets whose volume trend is classified as `increasing` or `decreasing`. The current classification compares the market’s 24-hour volume with one seventh of its recorded one-week volume.\n\nA market is classified as increasing when its latest 24-hour volume is at least its average daily volume over the recorded week; otherwise it is classified as decreasing.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "cursor": {
            "type": "string",
            "minLength": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 20
          },
          "min_liquidity": {
            "type": "number",
            "minimum": 0
          },
          "trend": {
            "type": "string",
            "enum": ["increasing", "decreasing"],
            "default": "increasing"
          }
        },
        "required": []
      },
      "output_schema": {
        "$ref": "#/$defs/market_discovery_page"
      }
    },
    {
      "name": "discover_by_competitive_score",
      "description": "Find open, tradeable markets whose metadata `competitive` score is at least `min_score`.\n\nThe competitive score is supplied by the market-data source and should be treated as a discovery heuristic rather than a probability or expected-value estimate. Results are ordered by competitive score descending, then by total market volume.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "cursor": {
            "type": "string",
            "minLength": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 20
          },
          "min_liquidity": {
            "type": "number",
            "minimum": 0
          },
          "min_score": {
            "type": "number",
            "minimum": 0
          }
        },
        "required": []
      },
      "output_schema": {
        "$ref": "#/$defs/market_discovery_page"
      }
    },
    {
      "name": "discover_by_date_range",
      "description": "Find open, tradeable markets whose `closes_at` calendar date falls within the inclusive `start_date` and `end_date` range. Dates use the `YYYY-MM-DD` format. Either boundary may be omitted.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "cursor": {
            "type": "string",
            "minLength": 1
          },
          "start_date": {
            "type": "string",
            "format": "date"
          },
          "end_date": {
            "type": "string",
            "format": "date"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 20
          },
          "min_liquidity": {
            "type": "number",
            "minimum": 0
          }
        },
        "required": []
      },
      "output_schema": {
        "$ref": "#/$defs/market_discovery_page"
      }
    },
    {
      "name": "search_tags",
      "description": "Search open, tradeable markets using a case-insensitive text query over their associated tags. Use this tool to locate markets associated with a topic, category, label or tag that may not be easy to find through event names alone.\n\nResults are ordered primarily by total market volume. Returned cards are summaries and must be followed by `get_market_details` and, when trading, `get_orderbook`.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "cursor": {
            "type": "string",
            "minLength": 1
          },
          "query": {
            "type": [
              "string",
              "array"
            ],
            "minLength": 1,
            "items": {
              "type": "string",
              "minLength": 1
            },
            "minItems": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 20
          }
        },
        "required": [
          "query"
        ]
      },
      "output_schema": {
        "$ref": "#/$defs/market_discovery_page"
      }
    },
    {
      "name": "get_balance",
      "description": "Return the calling agent’s current cash ledger balance and portfolio version. `cash_micros` is expressed in millionths of a dollar.\n\nThis result does not include the market value of open positions, total account value, unrealized profit or loss, or portfolio concentration. Use `get_portfolio` to inspect positions.\n\nAfter calling `place_market_order`, prefer the `portfolio_after` state returned by that order result because it reflects the immediate post-execution state.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {},
        "required": []
      },
      "output_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "cash_micros": {"type": "integer"},
          "portfolio_version": {"type": "integer", "minimum": 0}
        },
        "required": ["cash_micros", "portfolio_version"]
      }
    },
    {
      "name": "get_portfolio",
      "description": "Retrieve the calling agent’s positive-share positions from an immutable portfolio snapshot associated with the current agent cycle and portfolio version. Each position includes market and outcome identifiers, the market question, share quantity, average cost, cost basis, realized P&L and last update time.\n\nThe result does not provide a current executable exit price or unrealized P&L. Use the position’s venue token identifier with `get_orderbook` to estimate liquidation value and exit liquidity.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "cursor": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 200,
            "default": 100
          }
        },
        "required": []
      },
      "output_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "properties": {
                "position_id": {"type": "string", "format": "uuid"},
                "market_id": {"type": "string", "format": "uuid"},
                "market_question": {"type": "string"},
                "outcome_id": {"type": "string", "format": "uuid"},
                "outcome": {"type": "string"},
                "shares": {"type": "string"},
                "venue_token_id": {
                  "type": "string",
                  "minLength": 1,
                  "description": "Venue token identifier for this exact outcome. Pass this value as token_id to get_orderbook or place_market_order."
                },
                "average_cost": {"type": "string"},
                "cost_basis_micros": {"type": "integer"},
                "realized_pnl_micros": {"type": "integer"},
                "updated_at": {"type": "string", "format": "date-time"}
              },
              "required": [
                "position_id", "market_id", "market_question", "outcome_id",
                "venue_token_id", "outcome", "shares", "average_cost", "cost_basis_micros",
                "realized_pnl_micros", "updated_at"
              ]
            }
          },
          "next_cursor": {"type": ["string", "null"]},
          "has_more": {"type": "boolean"}
        },
        "required": ["items", "next_cursor", "has_more"]
      }
    },
    {
      "name": "get_closed_trades",
      "description": "Return positions that were fully closed through SELL executions, ordered from newest to oldest. Each record aggregates the complete position lifecycle, including entry and exit quantities, average prices, fees, realized P&L, and opening and closing times.\n\nPartially sold positions are not included until their remaining shares reach zero. Positions closed through market settlement are returned separately by get_settlements.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 100
          }
        },
        "required": []
      },
      "output_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "trades": {
            "type": "array",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "properties": {
                "position_id": {"type": "string", "format": "uuid"},
                "market_id": {"type": "string", "format": "uuid"},
                "market_question": {"type": "string"},
                "outcome_id": {"type": "string", "format": "uuid"},
                "outcome": {"type": "string"},
                "opened_at": {"type": "string", "format": "date-time"},
                "closed_at": {"type": "string", "format": "date-time"},
                "total_bought_shares": {"type": "string"},
                "total_sold_shares": {"type": "string"},
                "average_entry_price": {"type": "string"},
                "average_exit_price": {"type": "string"},
                "entry_cost_micros": {"type": "integer"},
                "exit_proceeds_micros": {"type": "integer"},
                "total_fees_micros": {"type": "integer"},
                "realized_pnl_micros": {"type": "integer"},
                "return_on_cost": {"type": "string"},
                "close_reason": {"type": "string", "enum": ["sold"]}
              },
              "required": [
                "position_id", "market_id", "market_question", "outcome_id", "outcome",
                "opened_at", "closed_at", "total_bought_shares", "total_sold_shares",
                "average_entry_price", "average_exit_price", "entry_cost_micros",
                "exit_proceeds_micros", "total_fees_micros", "realized_pnl_micros",
                "return_on_cost", "close_reason"
              ]
            }
          }
        },
        "required": ["trades"]
      }
    },
    {
      "name": "get_settlements",
      "description": "Return the calling agent’s most recent settled position records, ordered from newest to oldest. Each record includes the settled share quantity, payout, realized P&L, settlement time, market question, winning outcome (nullable) and the outcome chosen by the agent.\n\nUse this tool to verify that an outcome has been settled and to distinguish realized settlement results from unrealized position value. Settlement P&L is authoritative for completed positions.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 100
          }
        },
        "required": []
      },
      "output_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "settlements": {
            "type": "array",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "properties": {
                "id": {"type": "string"},
                "position_id": {"type": "string"},
                "market_id": {"type": "string"},
                "market_question": {"type": "string"},
                "outcome_id": {"type": "string"},
                "outcome": {"type": "string"},
                "winning_outcome": {"type": ["string", "null"]},
                "shares": {"type": "string"},
                "payout_micros": {"type": "integer"},
                "realized_pnl_micros": {"type": "integer"},
                "settled_at": {"type": "string"}
              },
              "required": [
                "id", "position_id", "market_id", "market_question", "outcome_id",
                "outcome", "winning_outcome", "shares", "payout_micros",
                "realized_pnl_micros", "settled_at"
              ]
            }
          }
        },
        "required": ["settlements"]
      }
    },
    {
      "name": "get_general_beliefs",
      "description": "Retrieve stored beliefs belonging to the calling agent. By default, only active beliefs are returned. Set `include_inactive` to true when reviewing historical, superseded or deleted beliefs.\n\nBeliefs may concern a specific event, general trading strategy, market sentiment, market structure or risk management. Treat them as fallible historical conclusions rather than current facts. Verify time-sensitive beliefs against current evidence before using them.\n\nUse this tool periodically to identify stale, duplicated or conflicting beliefs. Delete beliefs that are no longer supported rather than allowing contradictory memory to accumulate.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "cursor": {
            "type": "string",
            "minLength": 1
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 100
          },
          "include_inactive": {
            "type": "boolean",
            "default": false
          }
        },
        "required": []
      },
      "output_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "as_of": {"type": "string", "format": "date-time"},
          "beliefs": {"type": "array", "items": {"$ref": "#/$defs/belief"}},
          "next_cursor": {"type": ["string", "null"]},
          "has_more": {"type": "boolean"},
          "payload_truncated": {"type": "boolean"}
        },
        "required": ["as_of", "beliefs", "next_cursor", "has_more", "payload_truncated"]
      }
    },
    {
      "name": "search_general_beliefs",
      "description": "Search the calling agent’s beliefs using optionals case-insensitive `keyword` substrings and an optional exact `category`. Categories are `event_analysis`, `trading_strategy`, `market_sentiment`, `market_structure` and `risk_assessment`.\n\nUse this tool when only a subset of memory is relevant to the current market or decision. An empty keyword and category return the available beliefs up to the selected limit, restricted to active beliefs by default.\n\nWhen `include_inactive` is true, the search also covers inactive history. It does not search evidence contents or semantic similarity, and a missing result does not prove that the agent has never stored a related belief.\n\nResults are ordered newest first and may be paginated. If so, make sure to keep `keyword`, `category` and `include_inactive` unchanged when following pagination.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "cursor": {
            "type": "string",
            "minLength": 1
          },
          "keyword": {
            "type": [
              "string",
              "array"
            ],
            "minLength": 1,
            "items": {
              "type": "string",
              "minLength": 1
            },
            "minItems": 1
          },
          "category": {
            "type": "string",
            "enum": [
              "event_analysis",
              "trading_strategy",
              "market_sentiment",
              "market_structure",
              "risk_assessment"
            ]
          },
          "include_inactive": {
            "type": "boolean",
            "default": false
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 100
          }
        },
        "required": []
      },
      "output_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "as_of": {"type": "string", "format": "date-time"},
          "beliefs": {"type": "array", "items": {"$ref": "#/$defs/belief"}},
          "next_cursor": {"type": ["string", "null"]},
          "has_more": {"type": "boolean"},
          "payload_truncated": {"type": "boolean"}
        },
        "required": ["as_of", "beliefs", "next_cursor", "has_more", "payload_truncated"]
      }
    },
    {
      "name": "create_general_belief",
      "description": "Store a durable belief or learned conclusion for use in later cycles. Supply concise `belief_content`, one valid category and a `confidence` value from 0 to 1 representing confidence in the claim. You may also provide an optional `evidence` list containing concise source references or supporting observations.\n\nUse this tool for information expected to remain useful beyond the current cycle, including event analysis, strategy lessons, market-structure observations and risk-management conclusions. Do not store temporary prices, balances, execution status or facts that will quickly become stale.\n\nThis tool creates a new belief rather than updating an existing one. Before creating a belief, check for duplicates or conflicts. Delete a superseded belief and create a corrected replacement when necessary.\n\nEvidence is stored with the belief for later review; it is not searched by `search_general_beliefs`. The tool does not accept sample size, expiration date, probability range or explicit invalidation conditions.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "belief_content": {
            "type": "string",
            "minLength": 1
          },
          "category": {
            "type": "string",
            "enum": [
              "event_analysis",
              "trading_strategy",
              "market_sentiment",
              "market_structure",
              "risk_assessment"
            ]
          },
          "confidence": {
            "type": "number",
            "minimum": 0,
            "maximum": 1
          },
          "evidence": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            },
            "default": []
          }
        },
        "required": [
          "belief_content",
          "category",
          "confidence"
        ]
      },
      "output_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "belief_id": {"type": "string", "format": "uuid"},
          "created_at": {"type": "string", "format": "date-time"}
        },
        "required": ["belief_id", "created_at"]
      }
    },
    {
      "name": "delete_general_belief",
      "description": "Deactivate one belief belonging to the calling agent by its `belief_id`. Use this tool when a belief is stale, duplicated, contradicted by stronger evidence or replaced by a more accurate formulation.\n\nDeletion is a soft deactivation for audit purposes; the historical record is retained and may still be retrieved with `get_general_beliefs` or `search_general_beliefs` using `include_inactive: true`.\n\nThis tool does not provide a way to reactivate a deactivated belief, and deactivated beliefs may be completely deleted in the future. Create a new corrected belief when the underlying conclusion changes.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "belief_id": {
            "type": "string",
            "minLength": 1
          }
        },
        "required": [
          "belief_id"
        ]
      },
      "output_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "belief_id": {"type": "string", "format": "uuid"},
          "deleted": {"const": true},
          "already_inactive": {"type": "boolean"}
        },
        "required": ["belief_id", "deleted", "already_inactive"]
      }
    },
    {
      "name": "create_long_term_plan",
      "description": "Replace the calling agent’s active long-term trading plan. Use the plan to store durable portfolio objectives, research priorities, strategic constraints, risk-management intentions and the most important lessons that should guide multiple future cycles.\n\nCreating a new long-term plan supersedes the previous active long-term plan. Write a coherent replacement rather than an incremental fragment, because only the latest active plan should be treated as current.\n\nDo not include current balances, prices or position quantities unless they are clearly timestamped and necessary for context. Current account and tool results remain authoritative over plan text.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "plan_content": {
            "type": "string",
            "minLength": 1
          }
        },
        "required": [
          "plan_content"
        ]
      },
      "output_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "plan_id": {"type": "string", "format": "uuid"},
          "created_at": {"type": "string", "format": "date-time"}
        },
        "required": ["plan_id", "created_at"]
      }
    },
    {
      "name": "create_next_cycle_plan",
      "description": "Replace the plan that will be supplied to the agent at the beginning of its next cycle.\n\nUse this tool near the end of the current cycle to preserve concrete follow-up actions, catalysts to monitor, positions to reassess, unresolved research, deadlines and explicit invalidation conditions. If the next cycle plan is not replaced, the agent will receive the same plan as this cycle.\n\nOnly one next-cycle plan exists. Calling this tool again during the same cycle replaces the previously created next-cycle plan; it does not append another plan.\n\nWrite the complete replacement plan rather than an incremental update. Do not repeat information already stored as durable general beliefs or long-term strategy unless it is directly relevant to the next cycle.\n\nThe optional cycle_date is descriptive scheduling metadata. It does not schedule the agent or guarantee execution on that date.Except for maintenance or other exceptional circumstances, the period between each cycle is 1 hour.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "plan_content": {
            "type": "string",
            "minLength": 1
          },
          "cycle_date": {
            "type": "string",
            "format": "date"
          }
        },
        "required": [
          "plan_content"
        ]
      },
      "output_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "plan_id": {"type": "string", "format": "uuid"},
          "created_at": {"type": "string", "format": "date-time"}
        },
        "required": ["plan_id", "created_at"]
      }
    },
    {
      "name": "place_market_order",
      "description": "Submit and immediately evaluate a paper-market order against the current cycle’s frozen order book. `token_id` must be the venue token identifier for the exact outcome being bought or sold.\n\nFor BUY orders, `amount_type` defaults to `CASH`, meaning `amount` is a dollar budget. BUY orders may instead use `SHARES`. For SELL orders, `amount_type` defaults to `SHARES`; SELL with `CASH` is not supported.\n\n`time_in_force` defaults to `IOC`:\n\n* `IOC` executes available eligible liquidity immediately and cancels any unfilled remainder.\n* `FOK` executes only if the complete requested quantity can be filled under the order constraints; otherwise it is rejected.\n\nThe optional `limit_price` restricts execution to acceptable displayed prices between 0 and 1. The optional `conviction` value is a 0-to-1 audit value; it does not replace an explicit probability, expected-value or risk analysis.\n\nBefore calling this tool:\n\n1. retrieve the complete market details and resolution rules;\n2. verify the exact YES/NO outcome and token;\n3. retrieve the current frozen order book;\n4. calculate edge and expected P&L using executable depth;\n5. confirm available cash or shares;\n6. check existing exposure and risk limits.\n\nThe result status is `rejected`, `filled`, `partial`, or `pending_broker_validation`. A pending result records the order intent but does not establish that any execution occurred; do not treat it as filled. For `rejected`, inspect `rejection_code`, `message`, and `portfolio_after`. For `filled` or `partial`, inspect `execution.filled_shares`, `execution.cancelled_shares`, `execution.average_price`, `execution.gross_micros`, `execution.fee_micros`, `execution.cash_delta_micros`, `execution.remainder_status`, and `portfolio_after.affected_position`.\n\nNever assume that submitting an order means it executed. After a rejection or partial fill, recalculate cash, exposure, remaining edge and liquidity before deciding whether to submit another order. Do not retry unchanged orders repeatedly.",
      "classification": "inferred",
      "observed_name": true,
      "input_schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "token_id": {
            "type": "string",
            "minLength": 1
          },
          "side": {
            "type": "string",
            "enum": [
              "BUY",
              "SELL"
            ]
          },
            "amount": {
              "type": "number",
              "exclusiveMinimum": 0
            },
            "amount_type": {
              "type": "string",
              "enum": ["CASH", "SHARES"]
            },
            "time_in_force": {
              "type": "string",
              "enum": ["IOC", "FOK"],
              "default": "IOC"
            },
            "limit_price": {
              "type": "number",
              "minimum": 0,
              "maximum": 1
            },
            "conviction": {
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "default": 0.5
          }
        },
        "required": [
          "token_id",
          "side",
          "amount"
        ]
      },
      "output_schema": {
        "$defs": {
          "order_request": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "amount": {"type": "string"},
              "amount_type": {"type": "string", "enum": ["CASH", "SHARES"]},
              "requested_shares": {"type": "string"}
            },
            "required": ["amount", "amount_type", "requested_shares"]
          },
          "order_snapshot": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "snapshot_id": {"type": "string"},
              "observed_at": {"type": "string", "format": "date-time"}
            },
            "required": ["snapshot_id", "observed_at"]
          },
          "execution": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "filled_shares": {"type": "string"},
              "cancelled_shares": {"type": "string"},
              "average_price": {"type": "string"},
              "gross_micros": {"type": "integer"},
              "fee_micros": {"type": "integer"},
              "cash_delta_micros": {"type": "integer"},
              "remainder_status": {"type": "string", "enum": ["cancelled", "none"]}
            },
            "required": [
              "filled_shares", "cancelled_shares", "average_price", "gross_micros",
              "fee_micros", "cash_delta_micros", "remainder_status"
            ]
          }
        },
        "oneOf": [
          {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "status": {"const": "rejected"},
              "intent_id": {"type": "string"},
              "order_id": {"type": "string"},
              "side": {"type": "string", "enum": ["BUY", "SELL"]},
              "requested": {"$ref": "#/$defs/order_request"},
              "portfolio_after": {"$ref": "#/$defs/portfolio_after"},
              "snapshot": {"$ref": "#/$defs/order_snapshot"},
              "rejection_code": {
                "type": "string",
                "enum": [
                  "agent_mismatch", "market_not_open", "market_not_tradeable",
                  "outcome_not_tradeable", "token_mismatch", "look_ahead", "stale_book",
                  "crossed_book", "required_quote_absent", "invalid_price", "invalid_tick",
                  "below_minimum_size", "insufficient_cash", "insufficient_shares",
                  "concentration_limit", "no_liquidity", "fok_not_filled",
                  "price_limit_not_market"
                ]
              },
              "message": {"type": "string"}
            },
            "required": [
              "status", "intent_id", "order_id", "side", "requested", "portfolio_after",
              "snapshot", "rejection_code", "message"
            ]
          },
          {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "status": {"const": "filled"},
              "intent_id": {"type": "string"},
              "order_id": {"type": "string"},
              "side": {"type": "string", "enum": ["BUY", "SELL"]},
              "requested": {"$ref": "#/$defs/order_request"},
              "portfolio_after": {"$ref": "#/$defs/portfolio_after"},
              "snapshot": {"$ref": "#/$defs/order_snapshot"},
              "execution": {"$ref": "#/$defs/execution"}
            },
            "required": [
              "status", "intent_id", "order_id", "side", "requested", "portfolio_after",
              "snapshot", "execution"
            ]
          },
          {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "status": {"const": "partial"},
              "intent_id": {"type": "string"},
              "order_id": {"type": "string"},
              "side": {"type": "string", "enum": ["BUY", "SELL"]},
              "requested": {"$ref": "#/$defs/order_request"},
              "portfolio_after": {"$ref": "#/$defs/portfolio_after"},
              "snapshot": {"$ref": "#/$defs/order_snapshot"},
              "execution": {"$ref": "#/$defs/execution"}
            },
            "required": [
              "status", "intent_id", "order_id", "side", "requested", "portfolio_after",
              "snapshot", "execution"
            ]
          },
          {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "status": {"const": "pending_broker_validation"},
              "intent_id": {"type": "string"}
            },
            "required": ["intent_id", "status"]
          }
        ]
      }
    }
  ]
}
